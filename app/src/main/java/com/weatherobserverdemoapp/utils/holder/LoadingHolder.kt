package com.weatherobserverdemoapp.utils.holder

import android.view.View
import androidx.recyclerview.widget.RecyclerView

class LoadingHolder(private val view: View) : RecyclerView.ViewHolder(view) {
}